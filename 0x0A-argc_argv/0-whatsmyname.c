#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * main - Prints the nmad of the program
 * @argc: An int value that stores the size of the commnad line arguments
 * @argv: A char array argument
 *
 * Return: 0
 */
int main(int  __attribute__ ((unused)) argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);

	return (0);
}
